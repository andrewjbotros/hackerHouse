<h1>Login</h1>
<%= form_for :session do |f| %>
	<div class="field">
		<%= f.label :email %>
		<%= f.text_field :email %>
	</div>

	<div class="field">
		<%= f.label :password %>
		<%= f.password_field :password %>
	</div>

	<div class="submit">
		<%= f.submit "Login" %>
	</div>

	<% end %>

